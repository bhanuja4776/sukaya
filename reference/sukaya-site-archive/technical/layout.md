# Layout

OBSERVED: GoDaddy responsive class system and media queries are preserved verbatim in raw HTML/CSS. Exact computed layout could not be verified because the browser network changed during inspection. See raw HTML and screenshots status in MISSING-DATA.md.
