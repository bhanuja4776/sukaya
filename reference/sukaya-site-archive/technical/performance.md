# Performance

All discovered CSS/JS/image/font binaries and sizes are recorded in asset-manifest.json. A Lighthouse run was NOT ACCESSIBLE because the controlled browser could not maintain a network connection.
